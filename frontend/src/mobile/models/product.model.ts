export interface Product {
  provider_nit: string;
  product_type: string;
  storage_conditions: string;
  temperature_required: number;
  name: string;
  batch: string;
  unit_value: number;
  created_at: string;
  sku: string;
  stock: number;
  expiration_date: string;
  status: string;
}
